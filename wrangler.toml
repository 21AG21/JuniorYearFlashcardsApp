name = "cards"
main = "worker/index.mjs"
compatibility_date = "2026-08-01"

# The app itself is served as static assets from this folder; only /api/* runs
# the Worker. One origin for page + API — no CORS in production at all.
[assets]
directory = "."
run_worker_first = ["/api/*"]

[[kv_namespaces]]
binding = "CARDS"
id = "39db93a67c96412aafca851c444cbb4a"
